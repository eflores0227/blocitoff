Currently Logged In User:<%= current_user.email %>


<ul>
  <%= render current_user.items %>
</ul>


<%= form_for [current_user, @item] do |f| %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
